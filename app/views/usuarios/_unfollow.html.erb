<%= form_for(current_usuario.active_relationships.find_by(followed_id: @usuario.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Deixar de seguir", class: "btn pull-right" %>
<% end %>
