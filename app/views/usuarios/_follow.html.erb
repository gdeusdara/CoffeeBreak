<%= form_for(current_usuario.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @usuario.id %></div>
  <%= f.submit "Seguir", class: "btn btn-primary pull-right" %>
<% end %>
