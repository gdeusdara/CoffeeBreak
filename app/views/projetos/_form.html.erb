
<%= simple_form_for @projeto, :html => { :multipart => true } do |f| %>
  <%= select_tag(:categoria_id, options_for_select(@categoria), :prompt => "Selecione uma Categoria") %>
	<%= f.input :titulo, label: "Título do Projeto" %>
	<%= f.input :descricao %>
	<%= f.input :instrucoes %>
  <%= f.file_field :foto_do_projeto %>
	<%= f.button :submit, :class => 'btn-custom2' %>
<% end %>
